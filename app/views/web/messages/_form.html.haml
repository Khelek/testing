= simple_form_for @message, default_form_options do |f|
  = f.error_notification

  = f.input :title
  = f.input :text

  = f.button :submit
  = link_to t('form.cancel'), messages_path, class: 'btn btn-default'
